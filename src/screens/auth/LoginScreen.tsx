import { Text, View } from 'react-native';

export default function LoginScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0F1115', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#F5F2E8' }}>Login — building today</Text>
    </View>
  );
}